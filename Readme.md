here are my anwsers

if nixshell

```nix develop```

make sure to have 

`AOC_SESSION_TOKEN` defined

you also need `curl` ()


zig build setup -Dyear=2024 -Dday=9
for a specific date
zig build for today

tests doesnt show anything when passed add '--summary all' to show successed tests
